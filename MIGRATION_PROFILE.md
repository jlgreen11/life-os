# MIGRATION_PROFILE

> **STUB** — no `./data/` v1 DBs found on this machine.
>
> Run `python scripts/profile_v1_dbs.py` on the Mac Mini (where the production DBs live) to regenerate this file with real numbers.

Expected DBs at `./data/`: events.db, entities.db, state.db, user_model.db, preferences.db.
